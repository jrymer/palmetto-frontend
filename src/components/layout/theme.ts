import { DefaultTheme } from 'styled-components';

const primary = '#5F4B66';
const primaryContrast = '#2C1320';
const secondary = '#A7ADC6';
const background = '#fff';
const offsetGrey = '#ccc';

const theme: DefaultTheme = {
  margin: '2em 2em',
  padding: '1em 1em',

  colors: {
    primary,
    primaryContrast,
    secondary,
    background,
    offsetGrey,
  },
};

export default theme;
